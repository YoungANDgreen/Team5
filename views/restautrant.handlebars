<div class="">
  <div class="">
    <h2>{{name}}</h2>
  </div>
  {{#each review as |review|}}
  <div class="dish col-md-5">
    <p>
      {{review.id}}
      {{review.name}}
    </p>
  </div>
  {{/each}}
</div>
